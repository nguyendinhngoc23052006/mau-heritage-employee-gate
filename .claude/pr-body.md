Fixes brand name typo (Mầu → Màu — the wordmark has an à with just a huyền tone, not the â-plus-huyền ầ I used) and redraws the placeholder logo mark from a birdwing/lens shape into a peaked-roof-plus-bowl silhouette closer to the actual mark.

## Changes

- `src/components/Brand/Wordmark.tsx` — displayed text `MẦU HERITAGE` → `MÀU HERITAGE`.
- `src/components/Brand/LogoMark.tsx` — `ariaLabel` default `Mầu Heritage` → `Màu Heritage`; SVG paths redrawn as two thin crescents (top arcs up = roof peak, bottom arcs down = bowl); comment updated.
- `public/logo-mark.svg` — same SVG path change for the favicon file.
- `index.html` — `<meta description>` and `<title>` `Mầu Heritage` → `Màu Heritage`.

Still a placeholder — I don't have your real SVG. If you drop the actual `logo-mark.svg` file into the repo (or paste the SVG XML in chat), I'll swap it in with a one-line change. The wordmark spelling is now correct.

## Self-check
- [x] base = main; exactly one PR
- [x] no migration in this PR
- [x] tests/lint/typecheck green — typecheck ✓, build ✓, 11/11 tests still pass
- [x] scripts named exactly `lint`, `typecheck`, `test`; [~] e2e not yet added
- [x] key read from `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY`; nothing hardcoded
- [~] no migration → no SQL block needed
- [x] no irreversible actions
- [x] no avoidable debt
- [~] no migration
- [~] reviewers not dispatched — 4-file typo/asset fix, orchestrator-verified
- [~] no subagents dispatched for this PR

## For you

**What changed:** wordmark displays "MÀU HERITAGE" instead of the mis-diacritic'd "MẦU HERITAGE", logo silhouette is closer to a roof-and-bowl than a birdwing, `<title>` and `<meta>` use the correct spelling.

**What you do next:** review the Cloudflare Pages preview and merge. Send me the real logo SVG when you can — I'll swap the placeholder in a one-line follow-up.

**How to roll it back:** Cloudflare Pages → Deployments → Rollback to the prior deployment.

---
_Generated by [Claude Code](https://claude.ai/code)_
