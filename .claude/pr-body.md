Closes two user-reported issues: the geoguard flow failing with "browser not allowed" (root-caused to `public/_headers` shipping `Permissions-Policy: geolocation=()` since first commit), and the mobile header stacked chaotically with a duplicate "+ Cửa hàng khác" nav entry.

## Self-check
- [x] base = main; exactly one PR
- [x] ≤ 1 migration file — no schema change in this PR (client + config + i18n only)
- [x] tests/lint/typecheck green; happy AND unhappy paths exercised
- [~] scripts named exactly `lint`, `typecheck`, `test`; and `e2e` if installed — e2e not yet added
- [x] key read from `VITE_SUPABASE_URL` and `VITE_SUPABASE_PUBLISHABLE_KEY`; nothing hardcoded; no secret in code
- [x] no new migration — no SQL to paste
- [x] irreversible actions guarded — no destructive writes in this PR
- [x] no avoidable debt; memory updated
- [x] reviewers ran — investigate → write → check → fix → final check (5 swarms); `.claude/review/*` refreshed for this PR
- [x] every subagent dispatched on a model below the orchestrator's

## For you

**What changed:**
- **Geoguard actually works now.** `public/_headers` line 5 changed from `Permissions-Policy: geolocation=(), microphone=(), camera=()` to `geolocation=(self), ...`. The empty allowlist was blocking geolocation for every origin including the site itself — Chromium/Safari refused to even show the permission prompt. That's the "browser not allowed" the tester saw. Camera/mic stay locked (nothing uses them).
- **GeofenceCard uses low-accuracy + 12s timeout** for the "Use my current location" button (setting a store pin doesn't need GPS-lock accuracy; indoors, high-accuracy times out too often). ClockPage keeps high-accuracy for actual on-site verification.
- **Denied-permission hint** appears under the "location denied" alert in both GeofenceCard and ClockPage with the browser-settings recovery path.
- **Google Maps hint** under the lat/lng inputs so a manager who can't get browser location still has a manual path.
- **Header cleanup:** removed Nav's `+ Cửa hàng khác` (duplicate of StoreSwitcher's add-store button — both went to `/onboarding?add=1`). Layout header split into two stable rows: [Wordmark | Locale + Signout] and [StoreSwitcher + StoreIdentity]. StoreSwitcher's label shortened to `+ Cửa hàng khác` (was 32 chars); full sentence kept as the button/dropdown option's title tooltip.
- **Select component:** `Select…` and `Search…` placeholders now go through i18n (were hardcoded English), plus a new optional `title` field on options so tooltips can surface for truncated labels.
- **i18n:** 4 new keys (`store.switcher.add_full`, `settings.geofence.manual_hint`, `geofence.denied_hint`, `common.search_placeholder`), 1 removed orphan (`nav.add_store`). 428/428 parity in vi + en.

**What you do next:**
1. Review the Cloudflare Pages preview.
2. Merge to main.
3. No SQL to paste — client + config only.
4. On mobile: open the preview → Settings → Store location → "Use my current location" → the browser should now prompt for permission (previously it refused silently). Tap Allow → lat/lng populate → Save → toggle Require and re-test Clock In from outside the radius.

**How to roll it back:**
Cloudflare Pages → Deployments → Rollback to the prior deployment. No schema changes.

---
_Generated by [Claude Code](https://claude.ai/code/session_01YbULxuZWdoENDmLDSSuo7M)_
