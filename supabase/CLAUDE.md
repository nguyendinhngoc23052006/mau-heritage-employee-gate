Migrations are append-only and UTC-named (`YYYYMMDDHHMMSS_description.sql`), and are applied by the `apply-migrations` workflow on merge to `main` — never by hand. See the root `CLAUDE.md` Migrations rule.
