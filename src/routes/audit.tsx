import type { RouteObject } from "react-router-dom";

// Stub. Haiku 4 replaces with AuditPage.
export const auditRoutes: RouteObject[] = [
  { path: "audit", element: <div className="p-6">Audit log (placeholder)</div> },
];
