import type { RouteObject } from "react-router-dom";

// Stub. Haiku 4 replaces with DashboardPage.
export const dashboardRoutes: RouteObject[] = [
  { index: true, element: <div className="p-6">Dashboard (placeholder)</div> },
];
